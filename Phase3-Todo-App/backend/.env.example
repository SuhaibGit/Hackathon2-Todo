# Hugging Face Inference Endpoints - Environment Variables
# Copy these values to your Hugging Face repository secrets

# Required: PostgreSQL database connection string
# Get this from your database provider (Neon, Supabase, etc.)
DATABASE_URL=postgresql://username:password@host:5432/dbname

# Required: Secret key for JWT token signing
# Generate with: openssl rand -hex 32
# Must be at least 32 characters long
BETTER_AUTH_SECRET=your-secret-key-min-32-chars-long

# Required: CORS allowed origins (JSON array)
# Include your Hugging Face frontend URL(s)
# Example: ["https://your-app.hf.space"]
ALLOWED_ORIGINS=["http://localhost:3000"]

# Optional: Gemini API key for AI chat features
# Get from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your-gemini-api-key-here

# Optional: Port (default: 8080)
# Hugging Face Inference Endpoints use port 8080 by default
PORT=8080
